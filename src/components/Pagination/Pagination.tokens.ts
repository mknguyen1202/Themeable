export const paginationTokens = {
    buttonColor: 'var(--text-primary)',
    activeColor: 'var(--intent-primary)',
    activeTextColor: '#ffffff',
    borderColor: 'var(--border-primary)',
    hoverBackgroundColor: 'var(--surface-sunken)',
    disabledColor: 'var(--text-tertiary)',
    borderRadius: 'var(--radius-md)',
} as const;
