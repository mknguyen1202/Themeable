export const breadcrumbTokens = {
    textColor: 'var(--text-secondary)',
    activeTextColor: 'var(--text-primary)',
    linkColor: 'var(--intent-info)',
    separatorColor: 'var(--text-tertiary)',
    fontSize: 'var(--font-size-sm)',
} as const;
