export const tooltipTokens = {
    backgroundColor: 'var(--text-primary, #1a1a1a)',
    textColor: 'var(--surface-base, #ffffff)',
    borderRadius: 'var(--radius-sm)',
    fontSize: 'var(--font-size-sm)',
    maxWidth: '300px',
    arrowSize: '6px',
} as const;
